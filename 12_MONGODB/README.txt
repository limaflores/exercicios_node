



# Run:
docker-compose up
docker exec -it mongodb mongo -u root -p example --authenticationDatabase admin
# To stop the containers, run this command in a separate terminal window.


# Para criar um bd
use nome_banco

